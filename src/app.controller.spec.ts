import { Test, TestingModule } from '@nestjs/testing';
import { StudentUploadController } from './student-upload.controller';
// import { StudentUploadService } from './student-upload-producer.service';

// describe('AppController', () => {
//   let appController: StudentUploadController;

//   beforeEach(async () => {
//     const app: TestingModule = await Test.createTestingModule({
//       controllers: [StudentUploadController],
//       providers: [StudentUploadService],
//     }).compile();

//     appController = app.get<StudentUploadController>(StudentUploadController);
//   });

//   describe('root', () => {
//     it('should return "Hello World!"', () => {
//       expect(appController.getHello()).toBe('Hello World!');
//     });
//   });
// });
